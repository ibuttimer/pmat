<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SelectTransferActivity" >

    <LinearLayout
        android:id="@+id/selectTransfer_layoutSendDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
    	android:orientation="horizontal" >
        
        <TextView
            android:id="@+id/selectTransfer_textViewSendDateLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="10dp"
            android:text="@string/addtransaction_transfer_start_date" />

        <TextView
            android:id="@+id/selectTransfer_textViewSendDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/textViewDefaultText" />
            
    </LinearLayout>

    <TextView
        android:id="@+id/selectTransfer_textViewTransfers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/addtransaction_configured_transfers"
        android:layout_below="@+id/selectTransfer_layoutSendDate" />
    
    <ListView
        android:id="@+id/selectTransfer_listViewTransfers"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" 
        android:layout_below="@+id/selectTransfer_textViewTransfers" >
    </ListView>

</RelativeLayout>